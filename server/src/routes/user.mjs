// import { Router } from "express";
// import { createUser, deleteUser, getUser, updateUser } from "../controllers/user.mjs";

// const router = Router();

// router.get('/', getUser);
// router.post('/', createUser);
// router.put('/:oauth_id', updateUser);
// router.delete('/', deleteUser);

// export default router;